/*
let is for changeable variable
const is for variables you do not want to change like PictureInPictureEvent,directory to a File.
*/
const example = [];
const example2 = {};
example.push(3);
example2.number = 1;
console.log(example);
console.log(example2);
//able to push(array) or asign property(object) but cant reasign to new variable